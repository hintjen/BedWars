plugins {
    // Lets Gradle auto-download a matching JDK for the configured Java toolchain
    // (needed so the 26.x profile can build/run on Java 25 without a manual JDK install).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'BedWars'
